[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-tutor"
version = "0.1.0"
description = "Tutor — knowledge anchors, TUTOR_JUMP navigation, learning progress"
license = {text = "MIT"}
requires-python = ">=3.8"
